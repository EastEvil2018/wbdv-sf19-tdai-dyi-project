import { SpotifyType } from '../../models/Search/SearchType';

const HomeReducer = (
    state = {

    }, action
) => {
    switch(action.type) {
        default:
            return {...state};
    }
}

export default HomeReducer;